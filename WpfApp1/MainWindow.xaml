<Window x:Class="BelayneCipherWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Шифр Билайна (Вариант 13)" 
        Height="550" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Шифр Билайна (Двухлинейный / Zigzag)" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Параметры -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Количество строк:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtRows" Width="80" Text="3" FontSize="14" 
                     HorizontalContentAlignment="Center"
                     ToolTip="Введите число от 2 и больше"/>
        </StackPanel>

        <!-- Исходный текст -->
        <GroupBox Grid.Row="2" Header="Исходный текст" Margin="0,0,0,10">
            <TextBox x:Name="txtInput" AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" FontSize="14" Margin="5"/>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="btnEncrypt" Content="Зашифровать →" Width="140" Height="40" 
                    Margin="10" FontSize="14" Click="btnEncrypt_Click"/>
            <Button x:Name="btnDecrypt" Content="← Расшифровать" Width="140" Height="40" 
                    Margin="10" FontSize="14" Click="btnDecrypt_Click"/>
            <Button x:Name="btnClear" Content="Очистить" Width="100" Height="40" 
                    Margin="10" FontSize="14" Click="btnClear_Click"/>
        </StackPanel>

        <!-- Результат -->
        <GroupBox Grid.Row="4" Header="Результат" Margin="0,10,0,0">
            <TextBox x:Name="txtOutput" AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" FontSize="14" Margin="5" IsReadOnly="True"/>
        </GroupBox>

        <!-- Строка состояния -->
        <StatusBar Grid.Row="5" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="txtStatus" Text="Готов к работе" Foreground="Green"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>